# Project-specific R8/ProGuard rules.
#
# The app currently relies mostly on Android framework APIs and direct Kotlin
# calls. Keep this file present so release and CI builds have a stable target
# for future keep rules.
